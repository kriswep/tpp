{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.0"
  },
  "scripts": {
    "estimation:dev": "lerna run dev --stream --scope 'estimation-*'",
    "estimation:start": "npx lerna run start --stream --scope 'estimation-*'",
    "estimation:build": "npx lerna run build --stream --scope 'estimation-*'",
    "estimation:test": "npx lerna run test --stream --scope 'estimation-*'"
  }
}
