import React from 'react';

import ScreenCenterer from './ScreenCenterer';
import Text from './Text';

const Listen = props => {
  return (
    <ScreenCenterer>
      <Text size="huge">Listen and ask!</Text>
    </ScreenCenterer>
  );
};

export default Listen;
